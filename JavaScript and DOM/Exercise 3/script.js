// Task: Use `getElementById` to select the input and paragraph elements.
// Add an event listener to the input to detect the "Enter" key, and update the paragraph with the input value when Enter is pressed.

// YOUR CODE GOES HERE: Use `getElementById` to select input and paragraph

// Add an event listener for the "Enter" key
inputElement.addEventListener("keydown", function(event) {
     if (event.key === "Enter") {
        // YOUR CODE GOES HERE;
        console.log("Enter Key is pressed");
     }
});
